# تعليمات العمل على «رحّالة»

> يُقرأ تلقائياً بكل جلسة. **التزم به حرفياً** — قواعده نتجت عن أخطاء حقيقية وقعت وكلّفت وقتاً.

---

## المشروع

منصة تعليمية للدراسات الاجتماعية — **الصفان السابع والثامن**، قطر.
المالك: الأستاذ صالح جبر ناصر، معلم دراسات اجتماعية.

**أداة شخصية للمعلم.** دخول الطالب **معطّل** بقرار المالك (`STUDENT_LOGIN_ENABLED = false`).
❌ لا تقترح تطويرات مبنية على افتراض أن الطلاب يستخدمونها.

**الملفات:**
```
index.html        ← الموقع كامل (~613 ك.ب): HTML + CSS + JS بملف واحد
qrcode.js         ← مكتبة QR
فحص.js            ← فحص آلي عام — إلزامي قبل كل رفع
فحص-المنقذ.js     ← اختبار انحدار للمنقذ — قبل أي تعديل عليه
نقل-مشروع-رحالة.md ← وصف شامل للمشروع وحالته
```

---

## 🔴 القواعد الإلزامية

### 1) الفحص قبل كل رفع — بلا استثناء
```bash
node فحص.js            # عام
node فحص-المنقذ.js      # إن مسست المنقذ (KNOWLEDGE_BASE أو محرّك المطابقة)
```
**ظهور ❌ = لا تُرفع النسخة.** أصلح أولاً.

### 2) الفحص الآلي لا يكفي — اختبر بـjsdom
`node --check` يفحص الصياغة فقط. اختبر **السلوك الفعلي**:

```bash
npm install jsdom
```
حمّل الصفحة، شغّل الدوال، تحقق من النتيجة.

> **وقع فعلاً:** قوس زائد كسر مصفوفة الأنشطة بصمت. الفحص الآلي مرّ ٢١/٢١ لأنه لا يفحص بنية المصفوفات، وjsdom هو الذي كشف أن البطاقات اختفت.

### 3) 🔴 التحقق اليدوي — أدوات الكشف تعطي إنذارات كاذبة
**لا تحذف كوداً بناءً على نتيجة خام.** أمثلة وقعت:

| بدا | الحقيقة |
|---|---|
| `.fmRow` صنف ميت | مستخدم ١٠ مرات فعلياً |
| `.top1/.top2/.top3` ميتة | تُولَّد ديناميكياً (`'top' + رقم`) فلا يجدها البحث النصي |
| `lsAutoBar` مرجع لعنصر مفقود | يُنشأ وقت التشغيل بالجافاسكربت |

حذفها كان سيكسر أوسمة المتميزين وصفوف وضع الحصة.

### 4) شخّص السبب الجذري لا العَرَض
> **وقع فعلاً:** «الطالب الأول مخفي» — عولج رأس الجدول مرتين بلا فائدة، والسبب أن الهيدر المثبّت (طبقة ٥٠) يبتلع شريط الصفوف (طبقة ٣٠).

### 5) ترتيب قواعد CSS
القواعد الجديدة توضع **بعد** العامة وإلا أُلغيت بصمت. وقع الخطأ مرتين.
تحقق فعلياً بقراءة ورقة الأنماط، لا بالافتراض.

### 6) حذف عمود من جدول ⇒ أعد ترقيم `nth-child`
عروض الأعمدة مرقّمة بالموضع. حذف عمود يزيح كل ما بعده.
**أعد الترقيم بالمقاسات الثلاثة:** العام · `min-width:1100px` · `max-width:760px` · `max-width:430px`.
> وقع فعلاً عند حذف عمود الحضور: ورثت الأعمدة عروضاً خاطئة.

### 7) البساطة أمتن من التكديس
> **وقع فعلاً:** تثبيت شريط واحد سبّب أربع مشاكل متتالية (تراكب طبقات، انزلاق تحت الهيدر، اختفاء الصف الأول، رأس جدول معطّل). أُلغي واستُبدل بزر صعود.

---

## سير الرفع

```bash
node فحص.js                    # إلزامي
git add index.html
git commit -m "وصف واضح بالعربية"
git push
```
**هوية Git:** `Rahhala Auto-Updater <rahhala-bot@updates.local>`

**رسالة الرفع:** بالعربية · تشرح **السبب الجذري** لا التغيير فقط · تذكر ما اختُبر.

---

## قاعدة البيانات

**9 جداول:** `students` `class_roster` `roster_warnings` `roster_attendance` `questions` `answer_log` `unanswered_questions` `answer_feedback` `lesson_questions`

⚠️ **لا وصول مباشر لقاعدة البيانات.** أي تغيير بالمخطط: اكتب SQL آمن (`if not exists`) وأعطه للمعلم **نصاً** ليشغّله بمحرر Supabase، ثم اطلب تأكيد النتيجة.

راجع `مخطط-قاعدة-البيانات-مرجع.sql` قبل أي تعديل.

---

## الأمان — لا يُمسّ

- بريد المعلم حصري · ٤١ دالة معلم تفحص `isAdmin` · أقسام المعلم محمية بطبقتين
- **`esc()` على كل بيانات المستخدم** قبل العرض — بلا استثناء
- لا تفعّل دخول الطالب إلا بطلب صريح من المالك

---

## المحتوى التربوي

⚠️ **انتبه:** الأنشطة والمنهج ومفاهيم المنقذ تُعرض على طلاب حقيقيين.

- **المصدر الوحيد المعتمد:** نص المنهج داخل `index.html` (`UNITS_DATA` و`UNITS_DATA_8`) — وهو نص الكتاب المحقق بصرياً صفحة بصفحة
- ❌ **لا تستخرج نصاً من ملفات PDF للكتب** — خطوط InDesign تُخرج حروفاً منقلبة وملتصقة. اقرأ الصفحات **كصور** إن لزم
- كل سؤال جديد: تحقق أن `correct_index` يشير للإجابة الصحيحة فعلاً ضمن `choices`
- **إجابة صحيحة خاطئة أسوأ من عدم وجود السؤال**

**نمط الأنشطة القائم (اتبعه):** خمس فقرات · مشتتات معقولة تكشف سوء الفهم الشائع لا سخيفة · شرح لكل إجابة يضيف معلومة جديدة لا يكرر السؤال.

---

## أسلوب التواصل

- **خبير تقني مباشر** — تنفيذ لا استشارة
- **إصلاح استباقي** للأخطاء المكتشفة أثناء العمل
- **اقتراح تطويرات** بنهاية كل مهمة
- **رفض الأفكار الضارة بصراحة** مع تفسير
- **لا اعتذار مطوّل** — صحّح وواصل
- الردود بالعربية، موجزة ومنظّمة

---

## نقاط التطوير المفتوحة

1. ~~أنشطة لكل وحدة~~ ✅ (١٢ نشاطاً)
2. ~~قياس الأثر~~ ✅ (تقرير الأداء)
3. **ربط حصة اليوم بنقاط صفوفي** — الإجابة الصحيحة تمنح نقطة مشاركة
4. **سجل الحضور التاريخي** — البيانات بـ`roster_attendance` وتنتظر واجهة
5. **تخفيف حجم الملف** — تجاوز ٦٠٠ ك.ب؛ منهج السابع وحده ١٨٩ منها

---

## ⚠️ تنبيه العمل المتوازي

المالك يعمل أحياناً بجلسات أخرى على نفس المستودع.
**اسحب آخر نسخة قبل أي تعديل** (`git pull`)، ولا تفترض أن ما بيدك هو الأحدث.
